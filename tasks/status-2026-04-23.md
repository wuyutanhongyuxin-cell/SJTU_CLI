## 📋 SJTU-CLI 当前状态（2026-04-23）

> 本文档是会话重启前的时间点快照，记录"代码到哪了、还没干什么、为什么"。
> 后续进度以 `tasks/todo.md` 为准，本文件冻结。

---

### ✅ 已完成

| 阶段        | 内容                                                                               | 验收状态                                                  |
| ----------- | ---------------------------------------------------------------------------------- | --------------------------------------------------------- |
| S0          | 骨架 + 配置 + Envelope                                                             | 通过                                                      |
| S1          | JAccount QR 扫码登录                                                               | 真实扫码验证过（todo.md 有记录：抓到 7 条 cookie 含 `JAAuthCookie`） |
| S2          | CAS 通用通道 + sub_session 缓存                                                    | 真实教务 SP 验证过（首次 19420 ms → 缓存 6 ms）           |
| S1/S2 瑕疵  | Cookie 唯一键升级 RFC 6265 三元组 `(name, domain, path)`                            | 联网查过 RFC 6265 §5.3                                    |
| S3a 代码    | 水源 OAuth2 通道 + 5 个只读命令（`latest` / `topic` / `inbox` / `search` / `login-probe`）+ 7 模块 ~1000 行代码 | 代码齐全，但**未真实手动验证**                            |

---

### ⚠️ 关键差距（S3a 未完工项）

1. **真实水源 checkpoint 零次跑过**
   - `C:/Users/16191/AppData/Roaming/sjtu-cli/` 目录都不存在，说明 `sjtu shuiyuan login-probe` / `latest` 等命令都没真打过水源
   - 代码里 CP-1 / CP-2 / CP-3 注释只是占位标记，不代表"已通过"

2. **文档与代码不同步**
   - `tasks/todo.md` 的 S3 章节仍写"教务(jwc)"，与代码已切 S3a 水源不一致

3. **两个文件超 200 行硬限**
   - `src/cli.rs` 232 行
   - `src/commands/shuiyuan.rs` 218 行
   - `CLAUDE.md` 写了是硬限，要立即拆

4. **测试刚刚全绿（25/25）**
   - 本会话唯一实际改动：修了 `bare_client()` 的代理继承问题（加 `.no_proxy()`）
   - 根因是本机 `HTTP_PROXY=http://127.0.0.1:10808`，reqwest 默认继承，把 mockito 127.0.0.1 请求劫持走了

---

### 🧾 元信息

- **Plan 文件**：`C:/Users/16191/.claude/plans/bubbly-bubbling-firefly.md`（最新，S3a 方案）
- **S3 路线图**（用户钦定）：
  - S3a 水源社区 shuiyuan.sjtu.edu.cn
  - S3b 消息中心
  - S3c 日程
  - S3d 办事大厅
  - S3e 生活服务（一卡通余额 / 宿舍电费等）
- **S3a 范围**：只读，写操作（reply / like / new-topic）默认 `--confirm` 二次确认（目前写操作 0 代码）
- **下一步决策**：先跑扫尾（文档同步 / 两个文件拆分 / lesson 记录），再跑真实 S3a checkpoint，最后详规 S3b–S3e
